<div>
  <!-- Lista de parceiros -->
  <% if (parceiros && parceiros.length > 0) { %>
  <table class="table">
    <thead>
      <tr>
        <th>ID</th>
        <th>Nome</th>
        <th>Telefone</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% parceiros.forEach(parceiro => { %>
      <tr>
        <td><%= parceiro.idParceiro %></td>
        <td><%= parceiro.nome %></td>
        <td><%= parceiro.telefone %></td>
        <td>
          <!-- Botão para adicionar novo parceiro -->
          <button class="btn btn-round btn-sm btn-primary btn-hover-outline" onclick="abrirModalGenerico('/parceiros/new', 'Novo Parceiro')" title="Adicionar Novo Parceiro">
            <span class="material-symbols-outlined icon-btn">person_add</span>
          </button>
          <!-- Botão para visualizar parceiro -->
          <button class="btn btn-round btn-sm btn-info" onclick="abrirModalGenerico('/parceiros/<%= parceiro.idParceiro %>', 'Dados do Parceiro')" title="Ver Parceiro">
            <span class="material-symbols-outlined icon-btn">visibility</span>
          </button>
          <!-- Botão para editar parceiro -->
          <button class="btn btn-round btn-sm btn-warning" onclick="abrirModalGenerico('/parceiros/<%= parceiro.idParceiro %>/edit', 'Editar os Dados')" title="Editar Parceiro">
            <span class="material-symbols-outlined icon-btn">edit</span>
          </button>
          <!-- Botão para excluir parceiro -->
          <form action="/parceiros/<%= parceiro.idParceiro %>?_method=DELETE" method="post" class="d-inline">
            <button class="btn btn-sm btn-danger" type="submit" title="Excluir Parceiro">
              <span class="material-symbols-outlined icon-btn">delete</span>
            </button>
          </form>
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  <% } else { %>
  <p class="text-white">Nenhum Parceiro encontrado.</p>

  <% } %>
</div>
<!-- Modal genérico -->
<div><%- include('../partials/_modal') %></div>