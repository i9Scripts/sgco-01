<div>
  <!-- Formulário de busca -->
  <form action="/pacientes/search" method="get" class="mb-3">
    <div class="input-group">
      <input type="text" name="query" class="form-control" placeholder="Buscar por nome ou CPF" required />
      <button class="btn btn-outline-secondary" type="submit">Buscar</button>
    </div>
  </form>
  <!-- Lista de pacientes -->
  <% if (pacientes && pacientes.length > 0) { %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Responsável</th>
        <th>Pacientes</th>
        <th>Anamnese</th>
      </tr>
    </thead>
    <tbody>
      <% pacientes.forEach(paciente => { %>
      <tr>
        <td><%= paciente.nome %></td>
        <td><%= paciente.responsavel %></td>
        <td>
          <!-- Botão para adicionar novo paciente -->
          <button class="btn btn-sm btn-primary btn-hover-outline" title="Adicionar Novo Paciente" onclick="abrirModalGenerico('/pacientes/new','Novo Paciente')">
            <span class="material-symbols-outlined icon-btn">person_add</span>
          </button>
          <!-- Botão para visualizar paciente -->
          <button class="btn btn-sm btn-info" title="Ver Paciente" onclick="abrirModalGenerico('/pacientes/<%= paciente.idPaciente %>', 'Dados do Paciente')">
            <span class="material-symbols-outlined icon-btn">visibility</span>
          </button>
          <!-- Botão para editar paciente -->
          <button class="btn btn-sm btn-warning" title="Editar Paciente" onclick="abrirModalGenerico('/pacientes/<%= paciente.idPaciente %>/edit', 'Editar Paciente')">
            <span class="material-symbols-outlined icon-btn">edit</span>
          </button>
          <!-- Botão para excluir paciente -->
          <form action="/pacientes/<%= paciente.idPaciente %>?_method=DELETE" method="post" class="d-inline" title="Excluir Paciente">
            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Tem certeza que deseja excluir?')">
              <span class="material-symbols-outlined icon-btn">delete</span>
            </button>
          </form>
        </td>

        <!-- Anamnese -->
        <td>
          <% if (paciente.anamneses && paciente.anamneses.length > 0) { 
            const anamnese = paciente.anamneses[0]; %>
          <span class="badge bg-success">Registrada</span>
          <button class="btn btn-sm btn-success" title="Nova Anamnese" onclick="abrirModalGenerico('/pacientes/<%= paciente.idPaciente %>/selecionar', 'Nova Anamnese')">
            <span class="material-symbols-outlined icon-btn">check</span>
          </button>
          <button class="btn btn-sm btn-info" title="Ver Anamnese" onclick="abrirModalGenerico('/anamneses/<%= anamnese.idAnam %>', 'Ver Anamnese')">
            <span class="material-symbols-outlined icon-btn">visibility</span>
          </button>
          <button class="btn btn-sm btn-warning" title="Editar Anamnese" onclick="abrirModalGenerico('/anamneses/<%= anamnese.idAnam %>/edit', 'Editar Anamnese')">
            <span class="material-symbols-outlined icon-btn">edit</span>
          </button>
          <% } else { %>
          <span class="badge bg-secondary">Não registrada</span>
          <button class="btn btn-sm btn-primary" title="Criar Nova Anamnese" onclick="abrirModalGenerico('/pacientes/<%= paciente.idPaciente %>/selecionar', 'Criar Nova Anamnese')">
            <span class="material-symbols-outlined icon-btn">add</span>
          </button>
          <% } %>
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  <% } else { %>
  <p class="text-white">Nenhum Paciente encontrado.</p>
  <a class="btn btn-secondary btn-hover-outline" href="/pacientes/new" title="Voltar">
    <span class="material-symbols-outlined icon-btn">arrow_back</span> Voltar
  </a>
  <% } %>
</div>
<div><%- include('../partials/_modal') %></div>