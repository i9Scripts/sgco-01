<form action="/produtos" method="post" enctype="multipart/form-data">
  <div class="row form-group">
    <div class="col-md-6 mb-3">
      <input
        type="text"
        class="form-control"
        id="descricao"
        name="descricao"
        value="<%= formData && formData.descricao %>"
        required
        maxlength="100"
        autofocus
      />
      <label for="descricao">Descrição do Produto/Serviço</label>
    </div>
  </div>

  <div class="row form-group">
    <div class="col-md-3 mb-3">
      <input
        type="number"
        class="form-control"
        id="valor"
        name="valor"
        value="<%= formData && formData.valor %>"
        required
        step="0.01"
        min="0"
      />
      <label for="valor">Valor (R$)</label>
    </div>
    <div class="col-md-3 mb-3">
      <input
        type="number"
        class="form-control"
        id="quantidade"
        name="quantidade"
        value="<%= formData && formData.quantidade %>"
        required
        step="1"
        min="0"
      />
      <label for="quantidade">Quantidade em Estoque</label>
    </div>
  </div>

  <div class="row form-group">
    <div class="col-md-6 mb-3">
      <label for="imagem">Imagem (opcional)</label>
      <input type="file" class="form-control" id="imagem" name="imagem" accept="image/*" />
    </div>
  </div>

  <div class="fixed-buttons">
    <button class="btn btn-success btn-hover-outline" type="submit" title="Salvar novo produto">
      <span class="material-symbols-outlined icon-btn">save</span> Salvar
    </button>
    <a href="/produtos" class="btn btn-secondary btn-hover-outline" title="Cancelar e voltar">
      <span class="material-symbols-outlined icon-btn">cancel</span> Cancelar
    </a>
  </div>
</form>
